import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wordsonphone.app',
  appName: 'words-on-phone-app',
  webDir: 'dist'
};

export default config;
